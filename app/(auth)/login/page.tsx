/**
 * Login route for the authentication module.
 * This page is intentionally limited to the presentation of the login screen required in Sprint 1.
 */
import { LoginForm } from "@/components/auth/LoginForm";

export default function LoginPage() {
  return <LoginForm />;
}
