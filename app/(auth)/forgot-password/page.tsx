/**
 * Forgot password route for the authentication module.
 * This page exists as a visual mockup only and intentionally contains no submit or API behavior.
 */
import { ForgotPasswordForm } from "@/components/auth/ForgotPasswordForm";

export default function ForgotPasswordPage() {
  return <ForgotPasswordForm />;
}
